
- **Random Korean Character Generator**: Get a random Korean character for practice.
- **Romanization Feedback**: Enter the romanization of the character and get instant feedback.
- **Copy to Clipboard**: Easily copy the displayed character with the click of a button.

---

## Installation Instructions

Follow these steps to get the project running on your machine:

### 1. Clone the repository
In your terminal, run:
```bash
git clone https://github.com/mmfclarke/korean-character-practice.git
```
### 2. Navigate to the project folder
```bash
cd korean-character-practice
```
### 3. Set up a virtual envrionment (optional but recommended)
Create a virtual environment to manage dependencies:
```bash
python3 -m venv venv
```
### 4. Activate the virtual environment
For macOS/Linux:
```bash
source venv/bin/activate
```
For Windows:
```bash
venv\Scripts\activate
```
### 5. Install dependencies
To install the required dependencies (like tkinter):
```bash
pip install tk
```
### Run the project
To start the application:
```
python3 main.py
```

### Usage
This project is public for use. However, modifications and contributions 
are not currently accepted.
---

### **Next Steps**:
1. **Copy** the content above (from `# Korean Character Practice` to `Usage`).
2. **Paste** it directly into your `README.md` file.
3. **Commit and Push**:
   ```bash
   git add README.md
   git commit -m "Remove license and contribution section"
   git push
